---
import Section from '../shared/Section.astro'
import Item from '../shared/Item.astro'
import Title from '../shared/Title.astro'
import { getLevelMapping } from '@lib/levels.js'

const { languages, meta, resumeData } = Astro.props

const getFluencyConfig = (fluency) => {
  const mapping = getLevelMapping(fluency)
  const dotsMap = {
    'beginner': 2,
    'intermediate': 3, 
    'advanced': 4,
    'master': 5,
    'expert': 5
  }
  
  return { 
    dots: dotsMap[mapping.class.replace('level-', '')] || 3, 
    level: mapping.class.replace('level-', ''),
    label: fluency 
  }
}
---

<Section title={meta?.themeOptions?.sectionTitles?.languages || "Languages"} meta={meta} resumeData={resumeData}>
  <div class="languages-grid">
    {languages.map(lang => {
      const config = getFluencyConfig(lang.fluency)
      return (
        <Item>
          <div class="language-header">
            <Title>{lang.language}</Title>
            <div class="fluency-indicator">
              <div class="dots">
                {Array.from({ length: 5 }, (_, i) => (
                  <div 
                    class={`dot ${i < config.dots ? 'filled' : ''} level-${config.level}`}
                  ></div>
                ))}
              </div>
              <span class={`fluency-label level-${config.level}`}>{config.label}</span>
            </div>
          </div>
        </Item>
      )
    })}
  </div>
</Section>

<style>
  .languages-grid {
    display: grid;
    gap: var(--theme-spacing-lg);
    margin-bottom: 0;
    padding: var(--theme-spacing-md) 0;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    align-items: start;
  }

  .language-header {
    display: grid;
    grid-template-columns: 1fr auto;
    align-items: start;
    gap: var(--theme-spacing-md);
    margin-bottom: var(--theme-spacing-sm);
  }

  .language-header .title {
    margin: 0;
    font-size: 1.1rem;
  }

  .fluency-indicator {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: var(--theme-spacing-xs);
    max-width: 200px;
  }

  .dots {
    display: grid;
    grid-template-columns: repeat(5, 8px);
    gap: 4px;
  }

  .dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    transition: background-color var(--theme-transition-normal);
  }

  .fluency-label {
    font-size: var(--theme-font-size-sm);
    font-weight: var(--theme-font-weight-semibold);
    text-align: right;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.1);
  }

  /* Level-specific colors using theme variables */
  .level-beginner.filled {
    background-color: var(--theme-color-primary);
  }
  .level-beginner.fluency-label {
    color: var(--theme-color-primary);
  }

  .level-intermediate.filled {
    background-color: var(--theme-color-secondary);
  }
  .level-intermediate.fluency-label {
    color: var(--theme-color-secondary);
  }

  .level-advanced.filled {
    background-color: var(--theme-color-accent);
  }
  .level-advanced.fluency-label {
    color: var(--theme-color-accent);
  }

  .level-master.filled {
    background-color: var(--theme-color-primary);
  }
  .level-master.fluency-label {
    color: var(--theme-color-primary);
  }

  .dot:not(.filled) {
    background-color: var(--theme-surface-tertiary);
  }

  @media (max-width: var(--theme-breakpoint-lg)) {
    .languages-grid {
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    }
  }

  @media (max-width: var(--theme-breakpoint-md)) {
    .languages-grid {
      grid-template-columns: 1fr;
      gap: var(--theme-spacing-md);
    }
  }
</style> 
---
import ReferenceItem from './shared/ReferenceItem.astro';

export interface Props {
  items: any[];
  meta: any;
}

const { items, meta } = Astro.props;
---

<ReferenceItem items={items} meta={meta} /> 
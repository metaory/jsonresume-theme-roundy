---
const { class: className = '', variant = 'default' } = Astro.props

const variants = {
  default: 'divider-default',
  subtle: 'divider-subtle',
  accent: 'divider-accent',
  gradient: 'divider-gradient'
}
---

<div class={`divider ${variants[variant]} ${className}`}></div>

<style>
  .divider {
    height: 16px;
    display: block;
    position: relative;
    margin: var(--theme-spacing-2xl) 0;
    width: 100%;
    background-image: url('/assets/line.svg');
    background-size: 30px 26px;
    background-repeat: space;
    opacity: 0.6;
    transition: opacity var(--theme-transition-fast) var(--theme-easing);
  }

  .divider:hover {
    opacity: 1;
  }

  /* Variants */
  .divider-default {
    filter: hue-rotate(0deg) brightness(1);
  }

  .divider-subtle {
    opacity: 0.2;
    filter: grayscale(0.5);
  }

  .divider-subtle:hover {
    opacity: 0.4;
  }

  .divider-accent {
    filter: hue-rotate(180deg) saturate(1.5);
  }

  .divider-gradient {
    background: linear-gradient(90deg, transparent, var(--theme-color-primary), transparent);
    height: 2px;
    border-radius: var(--theme-border-radius-full);
    margin: var(--theme-spacing-xl) 0;
  }

  /* Responsive */
  @media (max-width: var(--theme-breakpoint-md)) {
    .divider {
      margin: var(--theme-spacing-xl) 0;
      height: 20px;
      background-size: 40px 30px;
    }
  }

  /* Print styles */
  @media print {
    .divider {
      opacity: 0.4;
    }

    .divider:hover {
      opacity: 0.4;
    }
  }
</style> 
---
import Tag from '@shared/Tag.astro'

const { 
  text, 
  icon, 
  href, 
  variant = 'outline',
  size = 'md',
  class: className = '' 
} = Astro.props
---

<Tag 
  text={text}
  icon={icon}
  href={href}
  variant={variant}
  size={size}
  class={className}
/> 
---
const { class: className = '' } = Astro.props
---

<p class={`description ${className}`}>
  <slot />
</p>

<style>
  .description {
    font-family: var(--font-description);
    color: var(--text-muted);
    line-height: 1.6;
    margin: var(--space-sm) 0;
  }
</style> 
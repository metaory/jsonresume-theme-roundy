---
import ProjectItem from './shared/ProjectItem.astro';

export interface Props {
  items: any[];
  meta: any;
}

const { items, meta } = Astro.props;
---

<ProjectItem items={items} meta={meta} /> 
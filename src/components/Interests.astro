---
import InterestItem from './shared/InterestItem.astro';

export interface Props {
  items: any[];
  meta: any;
}

const { items, meta } = Astro.props;
---

<InterestItem items={items} meta={meta} /> 